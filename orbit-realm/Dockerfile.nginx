# Dockerfile para servir la aplicación con nginx
FROM nginx:alpine

# Copiar los archivos construidos de la aplicación
COPY dist/spa/ /usr/share/nginx/html/

# Copiar la configuración personalizada de nginx
COPY nginx/conf.d/default.conf /etc/nginx/conf.d/default.conf

# Exponer el puerto 80
EXPOSE 80

# Comando por defecto (nginx ya está configurado para iniciarse automáticamente)
CMD ["nginx", "-g", "daemon off;"]
